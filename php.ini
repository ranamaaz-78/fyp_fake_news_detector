; Local dev overrides — use with: php -c php.ini artisan serve
; WAMP users: apply same values in WAMP tray → PHP → php.ini

upload_max_filesize = 128M
post_max_size = 256M
max_execution_time = 600
max_input_time = 600
memory_limit = 512M
